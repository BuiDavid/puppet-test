ername->/<-app name->: david.bui / puppet-test

  PkgInclude:
    - '*'

  PreInstall:
    - sudo curl -sL https://rpm.nodesource.com/setup_8.x | sudo bash -
    - sudo sudo yum install nodejs -y

  Start:
    - echo "Starting application"
    - /home/dtbui/myKeystoneApp node keystone.js
